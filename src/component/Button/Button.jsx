import { green } from '@mui/material/colors'
import React from 'react'

const Button = ({onClick}) => {
  return (
    <button onClick={onClick} className='oioi' >Add</button>
  )
}

export default Button